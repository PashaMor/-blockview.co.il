# dependencies
node_modules/

# Capacitor / Android build artifacts
android/app/build/
android/build/
android/.gradle/
android/captured/
android/app/release/
android/capacitor-cordova-android-plugins/
.gradle/

# built apps
*.apk
*.aab
*.keystore
*.jks
keystore.properties

# Apple App Store Connect API key — secret, upload to Codemagic only
*.p8
AuthKey_*.p8

# local / temp
build_*.txt
server.log
.DS_Store
Thumbs.db
.vercel

# never commit secrets (publishable key is fine; secret key must NOT be here)
.env
.env.*

# Importer handoff guide (contains a password — never commit)
IMPORTER-GUIDE.md
